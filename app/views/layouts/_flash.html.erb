<% flash.each do |key, value| %>
    <div class="fixed-top mt-5 container">
      <div id = "alertfadeout" class="alert <%= bs4_bgcolor_for(key) %> alert-dismissable fade show my-3">
        <%= value %>
      </div>
    </div>
  <% end %>